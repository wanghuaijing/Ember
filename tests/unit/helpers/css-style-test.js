import { cssStyle } from 'lf-manage/helpers/css-style';
import { module, test } from 'qunit';

module('Unit | Helper | css style');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cssStyle([42]);
  assert.ok(result);
});
