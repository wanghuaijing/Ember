import { objToArray } from 'lf-manage/helpers/obj-to-array';
import { module, test } from 'qunit';

module('Unit | Helper | obj to array');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = objToArray([42]);
  assert.ok(result);
});
