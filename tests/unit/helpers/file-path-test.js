import { filePath } from 'lf-manage/helpers/file-path';
import { module, test } from 'qunit';

module('Unit | Helper | file path');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = filePath([42]);
  assert.ok(result);
});
