import { formatInput } from 'lf-manage/helpers/format-input';
import { module, test } from 'qunit';

module('Unit | Helper | format input');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatInput([42]);
  assert.ok(result);
});
