import { toboolean } from 'lf-manage/helpers/toboolean';
import { module, test } from 'qunit';

module('Unit | Helper | toboolean');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toboolean([42]);
  assert.ok(result);
});
