import { formatPics } from 'lf-manage/helpers/format-pics';
import { module, test } from 'qunit';

module('Unit | Helper | format pics');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatPics([42]);
  assert.ok(result);
});
