import { arrayToObjlist } from 'lf-manage/helpers/array-to-objlist';
import { module, test } from 'qunit';

module('Unit | Helper | array to objlist');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayToObjlist([42]);
  assert.ok(result);
});
